import react from 'react';



const Spacer = ({leftId, rightId, onClick}) => {

    return (
        <span style={cursor: 'crosshair', backgroundColor: 'transparent', display: 'inline-block', width: '3px'}>

        </span>
    );
}