import {Box, Typography} from "@mui/material";


const HelpAdditionalNotes = () => {

    return (
        <Box>
            <Typography variant={"h5"}>Stuff about how to use the audio player</Typography>
        </Box>
    );
};

export default HelpAdditionalNotes;