import react from 'react';
import {Box, Typography} from "@mui/material";
import {HelpOutline} from "@mui/icons-material";


const HelpMode = () => {

    return (
        <Box>
            <Typography variant={"h5"}>Stuff about how to use help</Typography>
        </Box>
    );
};

export default HelpMode;