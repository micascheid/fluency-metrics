import {Box, Typography} from "@mui/material";


const HelpWorkspace = () => {

    return (
        <Box>
            <Typography variant={"h5"}>Stuff about how to use the workspace</Typography>
        </Box>
    );
};

export default HelpWorkspace;