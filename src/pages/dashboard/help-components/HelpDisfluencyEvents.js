import {Box, Typography} from "@mui/material";


const HelpDisfluencyEvents = () => {

    return (
        <Box>
            <Typography variant={"h5"}>Stuff about how to use the disfluency events</Typography>
        </Box>
    );
};

export default HelpDisfluencyEvents;