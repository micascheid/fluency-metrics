import {Box, Typography} from "@mui/material";


const HelpTranscription = () => {

    return (
        <Box>
            <Typography variant={"h5"}>Stuff about how to use the transcription</Typography>
        </Box>
    );
};

export default HelpTranscription;